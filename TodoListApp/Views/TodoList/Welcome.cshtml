@{
    ViewData["Title"] = "Welcome";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - TaskFlow</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            overflow-x: hidden;
        }

        .welcome-container {
            max-width: 900px;
            width: 100%;
            background: rgba(255, 255, 255, 0.98);
            border-radius: 30px;
            box-shadow: 0 30px 80px rgba(0, 0, 0, 0.3);
            padding: 60px 50px;
            text-align: center;
            animation: slideUp 0.8s ease;
        }

        .welcome-icon {
            width: 120px;
            height: 120px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 60px;
            margin: 0 auto 30px;
            animation: bounceIn 1s ease;
        }

        h1 {
            color: #2d3748;
            font-size: 3rem;
            font-weight: 800;
            margin-bottom: 20px;
        }

        .welcome-message {
            color: #4a5568;
            font-size: 1.2rem;
            line-height: 1.8;
            margin-bottom: 40px;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
        }

        .steps-container {
            margin: 50px 0;
            text-align: left;
        }

        .step {
            display: flex;
            align-items: flex-start;
            margin-bottom: 30px;
            padding: 25px;
            background: #f7fafc;
            border-radius: 15px;
            transition: all 0.3s ease;
            animation: fadeIn 1s ease;
        }

            .step:hover {
                transform: translateX(10px);
                background: #edf2f7;
            }

        .step-number {
            width: 50px;
            height: 50px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            font-weight: bold;
            flex-shrink: 0;
            margin-right: 20px;
        }

        .step-content h3 {
            color: #2d3748;
            font-size: 1.3rem;
            margin-bottom: 8px;
            font-weight: 700;
        }

        .step-content p {
            color: #718096;
            font-size: 1rem;
            line-height: 1.6;
        }

        .action-buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 40px;
        }

        .btn {
            padding: 18px 45px;
            font-size: 1.1rem;
            font-weight: 600;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
        }

            .btn-primary:hover {
                transform: translateY(-3px);
                box-shadow: 0 15px 35px rgba(102, 126, 234, 0.5);
            }

        .btn-secondary {
            background: transparent;
            color: #667eea;
            border: 2px solid #667eea;
        }

            .btn-secondary:hover {
                background: #667eea;
                color: white;
                transform: translateY(-3px);
            }

        .feature-highlight {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 50px;
            padding-top: 40px;
            border-top: 2px solid #e2e8f0;
        }

        .highlight-item {
            text-align: center;
            padding: 20px;
        }

        .highlight-icon {
            font-size: 2.5rem;
            margin-bottom: 10px;
        }

        .highlight-text {
            color: #4a5568;
            font-size: 0.95rem;
            font-weight: 600;
        }

        @@keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @@keyframes bounceIn {
            0% {
                opacity: 0;
                transform: scale(0.3);
            }

            50% {
                transform: scale(1.05);
            }

            100% {
                opacity: 1;
                transform: scale(1);
            }
        }

        @@keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        @@media (max-width: 768px) {
            .welcome-container {
                padding: 40px 25px;
            }

            h1 {
                font-size: 2.2rem;
            }

            .welcome-message {
                font-size: 1.1rem;
            }

            .step {
                flex-direction: column;
                text-align: center;
            }

            .step-number {
                margin: 0 auto 15px;
            }

            .btn {
                padding: 15px 35px;
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <div class="welcome-container">
        <div class="welcome-icon">👋</div>
        <h1>Welcome to TaskFlow!</h1>
        <p class="welcome-message">
            We're excited to have you here! TaskFlow is your personal productivity companion,
            designed to help you organize tasks, manage your time, and achieve your goals effortlessly.
        </p>

        <div class="steps-container">
            <div class="step">
                <div class="step-number">1</div>
                <div class="step-content">
                    <h3>Create Your First Task</h3>
                    <p>Start by adding your first task. It's quick and easy - just click "Create Task" and you're on your way!</p>
                </div>
            </div>

            <div class="step">
                <div class="step-number">2</div>
                <div class="step-content">
                    <h3>Organize & Prioritize</h3>
                    <p>Set priorities, add due dates, and categorize your tasks to stay on top of everything.</p>
                </div>
            </div>

            <div class="step">
                <div class="step-number">3</div>
                <div class="step-content">
                    <h3>Track Your Progress</h3>
                    <p>Mark tasks as complete and watch your productivity soar. Celebrate every accomplishment!</p>
                </div>
            </div>
        </div>

        <div class="action-buttons">
            <a href="@Url.Action("Create", "Todo")" class="btn btn-primary">Create Your First Task</a>
            <a href="@Url.Action("Index", "Todo")" class="btn btn-secondary">View All Tasks</a>
        </div>

        <div class="feature-highlight">
            <div class="highlight-item">
                <div class="highlight-icon">⚡</div>
                <div class="highlight-text">Lightning Fast</div>
            </div>
            <div class="highlight-item">
                <div class="highlight-icon">🎨</div>
                <div class="highlight-text">Beautiful Design</div>
            </div>
            <div class="highlight-item">
                <div class="highlight-icon">🔒</div>
                <div class="highlight-text">Secure & Private</div>
            </div>
        </div>
    </div>
</body>
</html>